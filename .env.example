# Opción A: Variables en la raíz del proyecto (Paginaweb/.env).
# Ahí se definen PGUSER_NOTICIAS, PGPASSWORD_NOTICIAS, PGDATABASE_NOTICIAS.
# El cron carga ese .env y exporta esas vars para los scripts R.

# Opción B: .env solo para noticias (este directorio). Copiar este ejemplo a .env y rellenar.
# Usado por prensa_scraping.R, cargar_noticias_postgres.R y cron si no hay vars _NOTICIAS en la raíz.

# Base de datos NOTICIAS (no confundir con camara_diputados del congreso)
PGHOST=localhost
PGPORT=5432
PGUSER=noticias
PGPASSWORD=
PGDATABASE=noticias_chile

# Crear la base una vez en PostgreSQL:
#   CREATE DATABASE noticias_chile;
